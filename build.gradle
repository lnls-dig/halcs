allprojects {
    apply plugin: 'c'

    model {
        buildTypes {
            debug
            production
        }

        binaries {
            all {
                cCompiler.args '-Wall', '-Wextra', '-Werror'

                if (buildType == buildTypes.debug)
                    cCompiler.args '-g'
            }
        }
    }
}
